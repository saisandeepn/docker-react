language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t saisandeep99/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true saisandeep99/docker-react npm run test